discord.py[voice]==2.7.1
# Explicit voice deps too, so managed hosts that parse requirements line-by-line
# still install Discord.py 2.7 DAVE voice support correctly.
PyNaCl>=1.5.0,<1.6
davey>=0.1.0
python-dotenv==1.1.1
aiosqlite==0.21.0
yt-dlp[default]
spotdl==4.5.2
Pillow==12.3.0
