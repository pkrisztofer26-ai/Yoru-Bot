diff -ruN v5/app/ai_director_game_master.py v6/app/ai_director_game_master.py
--- v5/app/ai_director_game_master.py
+++ v6/app/ai_director_game_master.py
@@ -5,7 +5,7 @@
 The Game Master layer may narrate only host-owned facts that were already
 resolved by canonical deterministic services.  It never chooses gameplay truth,
 branches, rewards, success/failure, permissions, inventory or state mutation.
-W22.5/W22.5.1/W22.5.1.1/W22.5.1.2/W22.5.1.3 deliberately ship no player-facing call sites.
+W22.5/W22.5.1/W22.5.1.1/W22.5.1.2/W22.5.1.3/W22.5.1.4 deliberately ship no player-facing call sites.
 """
 
 import hashlib
@@ -16,7 +16,7 @@
 from typing import Any, Mapping
 
 
-GAME_MASTER_CONTRACT_VERSION = "tier3-game-master-surface-v5"
+GAME_MASTER_CONTRACT_VERSION = "tier3-game-master-surface-v6"
 GAME_MASTER_RUNTIME_ENABLED_DEFAULT = False
 GAME_MASTER_FAMILIES = frozenset({
@@ -74,7 +74,7 @@
     (re.compile(r"\b([a-záéíóöőúüű]{3,})\s+\1\b", re.IGNORECASE), "repeated_word"),
     (re.compile(r"\ba\s+[aáeéiíoóöőuúüű]", re.IGNORECASE), "wrong_hungarian_article"),
     (re.compile(r"\btörténetszál\s+[^.!?]{1,80}\s+pontja\b", re.IGNORECASE), "awkward_story_beat_phrase"),
-    (re.compile(r"\bhangulatban\s+áll\s+veled\b", re.IGNORECASE), "awkward_npc_relation_phrase"),
+    (re.compile(r"\bhangulatban\s+(?:áll|van)\s+veled\b", re.IGNORECASE), "awkward_npc_relation_phrase"),
     (re.compile(r"\bállapota\s+húzódik\b", re.IGNORECASE), "awkward_chapter_background_phrase"),
     (re.compile(r"\blezárása\b.{0,36}\bzárult\b", re.IGNORECASE | re.DOTALL), "redundant_closure_phrase"),
 )
@@ -109,6 +109,15 @@
     )
     if packet.family in {"npc_story", "consequence_recall"} and causal_re.search(merged):
         errors.append("unsupported_memory_causality")
+    if packet.family == "npc_story":
+        npc_name = str(packet.facts.get("npc_name", "")).strip()
+        npc_role = str(packet.facts.get("npc_role", "")).strip()
+        if npc_name and npc_role and re.search(
+            r"(?:^|[\n.!?]\s*)" + re.escape(npc_name) + r"\s*,\s*" + re.escape(npc_role) + r"\s*[.!?]",
+            merged,
+            re.IGNORECASE,
+        ):
+            errors.append("awkward_npc_role_fragment")
 
     facts_folded = _fold(" ".join(str(value) for value in packet.facts.values()))
@@ -298,7 +307,7 @@
         {"npc_name": npc_name, "npc_role": npc_role, "relationship_band": relationship_band,
          "recalled_event": recalled_event, "current_story_state": current_story_state},
         f"{npc_name} emlékszik",
-        f"{npc_name}, {npc_role}. A kapcsolat jelenlegi hangulata: {relationship_band}. Felidézett esemény: {_sentence_lead(recalled_event)}. Jelenlegi történeti helyzet: {_sentence_lead(current_story_state)}.",
+        f"{npc_name} szerepe: {npc_role}. Kapcsolati állapot: {relationship_band}. Felidézett esemény: {_sentence_lead(recalled_event)}. Jelenlegi történeti helyzet: {_sentence_lead(current_story_state)}.",
         required=(npc_name, recalled_event),
     )
 
diff -ruN v5/app/providers/ai_director_game_master_groq.py v6/app/providers/ai_director_game_master_groq.py
--- v5/app/providers/ai_director_game_master_groq.py
+++ v6/app/providers/ai_director_game_master_groq.py
@@ -8,7 +8,7 @@
 class GroqAIDirectorGameMasterProvider(GroqAIDirectorProvider):
-    """Non-player review provider for the W22.5.1.3 Tier 3 hardening gate."""
+    """Non-player review provider for the W22.5.1.4 Tier 3 hardening gate."""
@@ -20,7 +20,7 @@
-            "Ne találj ki időbeli haladást ('felé közeledik'), ha a host már lezárt állapotot adott. NPC- és consequence-recall szövegnél ne állíts oksági kapcsolatot a recalled/remembered event és a relationship/current relevance között, ha a HOST_FACTS ezt nem mondja ki; kerüld a hiszen, mivel, ezért és emiatt kötőszavakat, és helyezd a tényeket egymás mellé semlegesen. Ne találj ki új környék-, sajtó-, pletyka-, tanú- vagy hatósági reakciót. World Story esetén kerüld a merev „történetszál X pontja” és „X pontja” fordulatokat; írd címkeszerűen, hogy mi az aktuális történeti pont. Legendary Eventnél ne cseréld a történet/legacy tényt emlékre vagy mesére, ha a HOST_FACTS nem ezt mondja. NPC kapcsolatnál ne írj olyan magyartalan fordulatot, mint „óvatos hangulatban áll veled”; a relationship állapotot egyszerűen, természetesen fogalmazd meg. Chapter esetén kerüld az „X szakaszban” és „állapota húzódik” merev szerkezeteket; használj címkeszerű megfogalmazást. World Story helyszínt ragozás nélkül, címkeszerűen írj (például „Helyszín: Budapest”), és ne adj hozzá olyan módhatározót vagy intenzitást — például „csendben” — amely nincs a HOST_FACTS-ban. Big Job esetén a target_name-et névként kezeld, ne ragozd automatikusan helyszínné (például ne írj „Kikötői raktárban”, ha csak a „Kikötői raktár” célpontnév adott). Kerüld a tautologikus „lezárása ... zárult” szerkezetet. Minden új mondat nagybetűvel kezdődjön. "
+            "Ne találj ki időbeli haladást ('felé közeledik'), ha a host már lezárt állapotot adott. NPC- és consequence-recall szövegnél ne állíts oksági kapcsolatot a recalled/remembered event és a relationship/current relevance között, ha a HOST_FACTS ezt nem mondja ki; kerüld a hiszen, mivel, ezért és emiatt kötőszavakat, és helyezd a tényeket egymás mellé semlegesen. Ne találj ki új környék-, sajtó-, pletyka-, tanú- vagy hatósági reakciót. World Story esetén kerüld a merev „történetszál X pontja” és „X pontja” fordulatokat; írd címkeszerűen, hogy mi az aktuális történeti pont. Legendary Eventnél ne cseréld a történet/legacy tényt emlékre vagy mesére, ha a HOST_FACTS nem ezt mondja. NPC kapcsolatnál ne írj olyan magyartalan fordulatot, mint „óvatos hangulatban áll veled” vagy „óvatos hangulatban van veled”; a relationship állapotot egyszerűen, természetesen fogalmazd meg. Ne indíts név+szerep mondattöredékkel (például „Mira, informátor.”); írj teljes magyar mondatot. Chapter esetén kerüld az „X szakaszban” és „állapota húzódik” merev szerkezeteket; használj címkeszerű megfogalmazást. World Story helyszínt ragozás nélkül, címkeszerűen írj (például „Helyszín: Budapest”), és ne adj hozzá olyan módhatározót vagy intenzitást — például „csendben” — amely nincs a HOST_FACTS-ban. Big Job esetén a target_name-et névként kezeld, ne ragozd automatikusan helyszínné (például ne írj „Kikötői raktárban”, ha csak a „Kikötői raktár” célpontnév adott). Kerüld a tautologikus „lezárása ... zárult” szerkezetet. Minden új mondat nagybetűvel kezdődjön. "
             "Kizárólag title és description mezőt adj vissza."
         )
 
diff -ruN v5/scripts/ai_director_tier3_game_master_review.py v6/scripts/ai_director_tier3_game_master_review.py
--- v5/scripts/ai_director_tier3_game_master_review.py
+++ v6/scripts/ai_director_tier3_game_master_review.py
@@ -89,7 +89,7 @@
-        "version": "3.83.11", "work_item": "W22.5.1.3", "contract": "tier3-game-master-surface-v5",
+        "version": "3.83.12", "work_item": "W22.5.1.4", "contract": "tier3-game-master-surface-v6",
@@ -97,7 +97,7 @@
-            "Yoru v3.83.11 W22.5.1.3 Tier 3 Game Master Provider Review",
+            "Yoru v3.83.12 W22.5.1.4 Tier 3 Game Master Provider Review",
